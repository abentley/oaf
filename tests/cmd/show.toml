bin.name = "oaf"
args = ["show", "--help"]
stdout = """
Summarize a commit or other object

Usage: oaf show [OPTIONS] [COMMIT]

Arguments:
  [COMMIT]  

Options:
      --name-only  Emit modified filenames only, not diffs
      --no-log     
  -h, --help       Print help
"""
