bin.name = "oaf"
args = ["revert", "--help"]
stdout = """
oaf-revert 0.1.5
Revert a previous commit

USAGE:
    oaf revert <source>

FLAGS:
    -h, --help       Prints help information
    -V, --version    Prints version information

ARGS:
    <source>    The commit to revert
"""
