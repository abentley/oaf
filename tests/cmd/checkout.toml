bin.name = "oaf"
args = ["checkout", "--help"]
stdout = """
Disabled to prevent accidentally discarding stashed changes

Usage: oaf checkout [OPTIONS] <BRANCH_NAME>

Arguments:
  <BRANCH_NAME>  The branch to switch to

Options:
  -b, --branch  
  -h, --help    Print help
"""
