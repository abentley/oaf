bin.name = "oaf"
args = ["cat", "--help"]
stdout = """
Output the contents of a file for a given tree

Usage: oaf cat [OPTIONS] <INPUT>

Arguments:
  <INPUT>  

Options:
  -t, --tree <TREE>  [default: ]
  -h, --help         Print help information
"""
