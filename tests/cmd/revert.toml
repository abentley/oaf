bin.name = "oaf"
args = ["revert", "--help"]
stdout = """
oaf-revert 
Revert a previous commit

USAGE:
    oaf revert <SOURCE>

ARGS:
    <SOURCE>    The commit to revert

OPTIONS:
    -h, --help    Print help information
"""
