bin.name = "oaf"
args = ["switch-prev", "--help"]
stdout = """
Switch to the previous branch in a sequence

Usage: oaf switch-prev [OPTIONS]

Options:
  -k, --keep  Switch without stashing/unstashing changes
  -h, --help  Print help information
"""
