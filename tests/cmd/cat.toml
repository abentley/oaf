bin.name = "oaf"
args = ["cat", "--help"]
stdout = """
oaf-cat 
Output the contents of a file for a given tree

USAGE:
    oaf cat [OPTIONS] <INPUT>

ARGS:
    <INPUT>    

OPTIONS:
    -h, --help           Print help information
    -t, --tree <TREE>    [default: ]
"""
