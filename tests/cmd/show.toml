bin.name = "oaf"
args = ["show", "--help"]
stdout = """
oaf-show 
Summarize a commit or other object

USAGE:
    oaf show [OPTIONS] [COMMIT]

ARGS:
    <COMMIT>    

OPTIONS:
    -h, --help         Print help information
        --name-only    Emit modified filenames only, not diffs
        --no-log       
"""
